"use client";

export function WalletSwitcher() {
  // Hide the wallet switcher since the user only wants automatic switching
  // based on browser extension changes, not manual switching between multiple wallets
  return null;
}
